# Lots of junk goes in here.
/caches/
/backups/

# Top secret stuff!
/link/.ssh/id_*
/link/.ssh/*id_rsa*
/link/.ssh/*.pem
/link/.ssh/known_hosts*
/link/.ssh/backup-configs/

# Don't commit rbenv temp files.
/link/.rbenv/shims/
/link/.rbenv/versions/
/link/.rbenv/version

# Don't commit tmux plugins, tpm will install them.
/link/.tmux/plugins

# Ignore any file with LOCAL in the name (good for per-machine vars/functions)
*LOCAL*

# Emacs
*.elc
/link/.emacs.d/session.*
/link/.emacs.d/meghanada
/link/.emacs.d/cider-history
*.bak

# Config files that are too volatile
config/okularrc
config/kdeglobals
config/konsolerc
